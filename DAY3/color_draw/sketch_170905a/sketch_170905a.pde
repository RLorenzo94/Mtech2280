void setup(){
  size(500,500);
  background(0); //black background
  
}
void draw(){
  // draw the menu
  fill(255,0,0);
  noStroke();
  rect(.1*width,.2*height,.25*width,.5*height);
  //create variable which is float
  //which means it has a decimal point
  //then use the equal sign to give it value
  
  float x =.1*width;
  float y =.2*height;
  float w =.25*width;
  float h =.5*height;
  
  println(.1*width,.2*height,.25*width,.5*height);
  
}